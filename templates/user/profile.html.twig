{% extends 'base.html.twig' %}

{% block nav_items %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('user_home') }}">Accueil</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
    </li>
{% endblock %}

{% block body %}
    <div>
        <img class="profile_image" src="{{ asset('img/anonyme.jpg') }}">
    </div>
    <h1>{{ user.username }}</h1>
    {#    <form>
        <label for="inputFirstname">Firstname</label>
        <input type="text" value="{{ user.firstname }}" name="firstname" id="inputFirstname" required>
        <label for="inputSurname">Surname</label>
        <input type="text" value="{{ user.surname }}" name="surname" id="inputSurname" required>
        <label for="inputEmail">Email</label>
        <input type="text" value="{{ user.email }}" name="email" id="inputEmail" required>

        <button class="btn btn-goovies mt-3" type="submit">
            Enregistrer
        </button>
    </form>#}
{% endblock %}